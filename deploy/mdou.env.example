# M豆 / Rainbond 组件环境变量示例
#
# 在 Rainbond 控制台 -> 组件 -> 环境变量 中配置：

DEFAULT_API_URL=https://llm-center.ali.modelbest.cn/llm/v1
ENABLE_API_PROXY=true
API_PROXY_URL=https://llm-center.ali.modelbest.cn/llm/v1
LOCK_API_PROXY=true

# 端口：80（与 Dockerfile EXPOSE 一致）
# 启动命令：无需额外设置（镜像自带 nginx CMD）
